const 
express = 
require('express');
const cors 
= 
require('cors');
const http 
= 
require('http');
const { 
Server } = 
require('socket.io');
require('dotenv').config();

const app 
= 
express();
const 
server = 
http.createServer(app);
const io = 
new 
Server(server, 
{ cors: { 
origin: 
"*" } });

app.use(cors());
app.use(express.json());

app.get('/', 
(req, res) 
=> 
res.json({ 
message: 
'🚀 Cafe 
Backend 
يعمل!' 
}));

app.post('/api/orders', 
(req, res) 
=> {
    const 
order = 
req.body;
    
order.orderNumber 
= 
`ORD-${Date.now()}`;
    
order.status 
= 
'pending';
    
io.emit('new-orders', 
[order]);
    
res.json({ 
success: 
true, 
data: 
order });
});

app.put('/api/orders/:id/status', 
(req, res) 
=> {
    
res.json({ 
success: 
true, 
message: 
'تم تحديث 
الحالة' 
});
});

const PORT 
= 
process.env.PORT 
|| 5000;
server.listen(PORT, 
() => {
    
console.log(`🚀 
Server: 
http://localhost:${PORT}`);
});
